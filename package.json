{
  "name": "rhythm-royale",
  "description": "Rhythm based battle royale game.",
  "scripts": {
    "start": "yarn --cwd backend/ run start",
    "build": "yarn --cwd frontend/ run build && yarn --cwd backend/ run build",
    "install": "yarn --cwd frontend/ install && yarn --cwd backend/ install"
  },
  "engines": {
    "node": "17.3.0",
    "yarn": "1.22.17"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MichaelClasby0/RhythmRoyale.git"
  }
}
